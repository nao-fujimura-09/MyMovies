    
      <%= image_tag current_user.get_profile_image(70,70), class:"user-icon" %>    
      <%= link_to edit_public_user_path(current_user), class:"link-body" do %>
        <div class="d-flex align-items-end">
          <h3 class ="pl-2 ">@<%= current_user.name %></h3>
        </div>
      <% end %>
    
      <p class="mt-3">
        フォロワー
          <%= link_to public_followers_path(user_id: user.id), class:"link-body" do %>
            <%= user.followers.where(status: false).count %>
          <% end %> |
        フォロー中
          <%= link_to public_followings_path(user_id: user.id), class:"link-body" do %>
            <%= user.followings.where(status: false).count %>
          <% end %>
      </p>
        
      <table class="table-borderless mt-5">
        <tr>
          <td class="font-weight-bold"><h4>お気に入りの映画</h4></td>
          </tr>  
          <tr>
          <td><%= current_user.favorite_movie %></td>
        </tr>
        </table></br>
    
        <table>
          <tr>
            <td class="font-weight-bold mt-3"><h4>好きなジャンル</h4></td>
          </tr>
          <tr>
            <td>
              <% Tmdb::Genre.movie_list.each do |movie_genre| %>
                <% current_user.favorite_genres.each do |favorite_genre|%>
                  <% if movie_genre.id == favorite_genre.genre_id %>
                    <%= movie_genre.name %> / 
                  <% end %>
                <% end %>
              <% end %>
            </td>
          </tr>
        </table>
    
        <h3 class="mt-5"><%= link_to"WATCH lIST", public_watch_lists_path, class:"link-body" %></h3>
        